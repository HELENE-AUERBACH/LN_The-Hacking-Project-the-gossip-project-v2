<h1>Crée un potin !</h1>
<p>Balance ton potin via le formulaire ci-dessous</p>

<% if !@gossip.nil? && @gossip.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
    <ul>
      <% @gossip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_tag url_for(action: 'create'), method: "post" do %>

  <%= label_tag 'Titre' %>
  <%= text_field_tag 'title', (@gossip.title if !@gossip.nil?), size: 20, maxlength: 14 %>
  <br /> <br />

  <%= label_tag 'Contenu' %>
  <%= text_area_tag 'content', (@gossip.content if !@gossip.nil?), size: "25x10" %>
  <br /> <br />

  <%= submit_tag "Enregistrer ce potin" %>

<% end %>
